# Copy to .env (never committed) and fill in with values from your Supabase
# project. For local development, run `supabase start` in backend/supabase/
# and use the "API URL" and "anon key" it prints.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
